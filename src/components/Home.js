import styled from 'styled-components';

const Home = (props) => {
    return <Container>
        Home
    </Container>
};

const Container = styled.main`
    position: relative;
`;

export default Home;